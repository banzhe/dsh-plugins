# Agent notes

This repo is a pnpm workspace of independent DSH Bundles. Package names are `@banzhe/dsh-<name>`. Directory names are unscoped: `plugins/<name>/`.

To add a plugin, use the project skill `add-dsh-plugin`.
